<ul class="nav nav-justified">
    {% for item in navigation %}
        <li>
            <a href="{{ content_path(item) }}" title="{{ item.title }}">{{ item.content.getPropertyValueByTagName('sulu.node.name') }}</a>
        </li>
    {% endfor %}
</ul>
